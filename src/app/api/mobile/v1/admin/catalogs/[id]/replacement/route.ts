import type { NextRequest } from 'next/server';
import { mobileJson, mobileOptions, requireJson } from '@/auth/lib/mobile';
import { CatalogDocumentService } from '@/auth/services/catalog-document.service';
import { catalogMobileActor, catalogMobileError } from '../../_utils';
export async function POST(request: NextRequest, { params }: { params: Promise<{ id: string }> }) { const actor = await catalogMobileActor(request); if (actor instanceof Response) return actor; if (!requireJson(request)) return mobileJson(request, { error: 'Content-Type must be application/json' }, { status: 415 }); try { const service = new CatalogDocumentService(); const id = (await params).id; const body = await request.json(); const existing = await service.getManaged(actor, id); if (!existing) return mobileJson(request, { error: 'Catalog not found.' }, { status: 404 }); if (body.operation === 'initialize') return mobileJson(request, { success: true, upload: await service.initializeUpload(actor, { title: existing.title, description: existing.description, categoryId: existing.categoryId, productId: existing.productId }) }); if (body.operation === 'finalize') return mobileJson(request, { success: true, catalog: await service.replacePdf(actor, id, body.publicId) }); return mobileJson(request, { error: 'Invalid operation.' }, { status: 400 }); } catch (error) { return catalogMobileError(request, error); } }
export function OPTIONS(request: NextRequest) { return mobileOptions(request); }
